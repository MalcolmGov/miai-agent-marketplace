import { ComingSoon } from "@/components/ComingSoon";

export default function RoadmapPage() {
  return (
    <ComingSoon
      title="Roadmap"
      description="What's shipping next across catalogue packs, connectors, and workspace features — updated as partnership priorities land."
    />
  );
}
