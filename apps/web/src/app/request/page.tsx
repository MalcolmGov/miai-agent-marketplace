import { ComingSoon } from "@/components/ComingSoon";

export default function RequestPage() {
  return (
    <ComingSoon
      title="Request a custom agent"
      description="Share the workflow, systems, and customer channel. Move Digital + MyInstantAI will scope a custom agent against the catalogue and your Azure tenancy."
      ctaHref="mailto:partners@myinstantai.com?subject=Custom%20agent%20request"
      ctaLabel="Email partnership team"
    />
  );
}
