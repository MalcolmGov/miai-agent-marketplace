import { ComingSoon } from "@/components/ComingSoon";

export default function AskPage() {
  return (
    <ComingSoon
      title="Ask AI"
      description="Your MyInstantAI assistant for marketplace questions, agent recommendations, and setup help — wired to the same token wallet."
    />
  );
}
