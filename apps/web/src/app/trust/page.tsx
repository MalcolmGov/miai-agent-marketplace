import { ComingSoon } from "@/components/ComingSoon";

export default function TrustPage() {
  return (
    <ComingSoon
      title="Trust & Security"
      description="Audit trails, data residency, connector scopes, and compliance posture for marketplace agents on your tenancy."
    />
  );
}
