import { ComingSoon } from "@/components/ComingSoon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Agent Admin"
      description="Workspace admin for rentals, roles, channel credentials, and policy — reserved for workspace operators."
    />
  );
}
