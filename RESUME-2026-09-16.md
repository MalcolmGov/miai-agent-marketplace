# Session State — 16 Sept 2026 (checkpoint before model switch)

> Read this first when resuming. One-screen summary, then details.

## Where things stand (verified live)

- **Production:** `miaiweb-production.up.railway.app` — commit `bb1839c`, health `ok`, Postgres hydrated, Redis (Upstash) ok, OIDC auth, OpenAI model mode, wallet `mock` (intentional for staging).
- **Local:** branch `bionic/fix-smoke-suite-gated-prod` in `~/Projects/miai-agent-marketplace` — E2E repair work committed here; **PR + merge pending** (see "Active task" below).
- **Railway CLI** is authenticated (`malcolmgov24@gmail.com`), linked to project `robust-enthusiasm` / production / `@miai/web`. Use `railway variables --json | jq -r 'keys[]'` to inspect env (never print values).
- **Zara VPS** (the embed/agent host, NOT the marketplace): `ssh aria` (46.224.42.163, key auth), app at `/opt/aria`, docker containers `personal-assistant-agent` (FastAPI :8000) + `zara-agent-runtime` (Node :8787).

## Active task — E2E suite repair (in progress on branch, PR pending)

**Goal:** make `pnpm smoke:staging` / handover suites green + truthful against the OIDC-gated prod.

**Root causes found & fixed (on the branch):**
1. B2B pivot moved the public catalogue from `/` to `/agents` — specs asserted the old location. Fixed `openCatalogue()` in `e2e/helpers.ts` (+ stale direct `goto("/…")` in catalogue-filters, partner-demo-journey, mobile-responsive).
2. Gated pages/APIs (`/agents/:id`, `/api/rent`, …) 401/307 under OIDC — specs relied on mock-roles headers that prod ignores. **New `e2e/auth.ts`** mints the standard HS256 business-session cookie (same shape as `signBusinessSession`; aud `miai:business:session`; cookie `miai_business_session`) from `E2E_SESSION_SECRET` (= server's `MIAI_SESSION_SECRET` or `OAUTH_TOKEN_SECRET` fallback) + optional `E2E_BUSINESS_EMAIL` (must be allowlisted; default `e2e-smoke@myinstantai.com` is covered by `MIAI_B2B_ALLOWED_DOMAINS=myinstantai.com`). Auth specs `test.skip` cleanly without the secret. `postJson`/`getJson` in helpers now take optional headers; `authHeaders()` for API specs; `authenticateContext(context, baseURL)` for UI specs.
3. `catalogue-filters` "Learn more" dialog test: page has 106 "Details" buttons; the first few are NOT in `#catalogue` (hero/rails, different behavior). Selector must be scoped: `page.locator("#catalogue").getByRole("button", { name: /Details/i })`. Verified via probe: in-catalogue click opens `[aria-labelledby="agent-detail-title"]` fine. Also wrapped click+assert in `expect(...).toPass()` (hydration race safety).
4. `e2e-staging.yml` now passes `E2E_SESSION_SECRET`/`E2E_BUSINESS_EMAIL` from GitHub secrets (empty → clean skips).
5. Docs updated: `TESTING.md` (new "Authenticated tests against OIDC" section + spec-map notes), `PROD_E2E.md` (Step 3 session-minting), `CONNECTOR_OAUTH.md` (corrected configured-provider list).

**Verified so far:** `@smoke` 15/15 WITH secret, 12 pass + 3 skip WITHOUT. `partner-demo-journey` (incl. browse→rent→chat→install) passes. `markets-za-fold` passes. `catalogue-filters` 3/3. Full handover suite was 50 passed / 33 failed before these fixes — re-run after finishing the remaining files.

**Remaining failures (33 across these files — pattern: thread `authHeaders()` into API specs, `authenticateContext` + skip-gate UI specs; some need content-drift investigation):**
- `e2e/api/chat-contract.spec.ts` (3), `dsar-contract.spec.ts` (2), `guardrail-refusals.spec.ts` (3), `insights-history.spec.ts` (2), `knowledge-contract.spec.ts`, `oauth-status.spec.ts`, `wallet-contract.spec.ts` — API contract specs; check each route's auth expectation (some may assert 401-without-auth — keep those; add session where the route is legitimately gated).
- `e2e/ui/sandbox-chat.spec.ts` — studio chat; needs session.
- `e2e/functional/studio-setup.spec.ts`, `b2b-onboarding.spec.ts`, `diligence-pages.spec.ts`, `e2e/uat/golive-hero-matrix.spec.ts`, `refusal-path.spec.ts` — investigate individually (gated pages vs content drift).

**Next steps:** finish fixes → `pnpm typecheck && pnpm test:web && pnpm --filter @miai/web lint && pnpm build:web` → full `handover:staging` green → PR (main is protected; required check `quality`; Railway auto-deploys on merge) → merge → verify `/api/version`.

## OAuth connectors — verified prod state (Railway env, Sept 16)

Configured: **Google** (Calendar/Gmail/Tasks/Contacts/Drive/YouTube), **Slack, HubSpot, Calendly, Spotify**. `APP_BASE_URL`, `OAUTH_STATE_SECRET`, `OAUTH_TOKEN_SECRET`, `OAUTH_TOKEN_STORE_PATH=/data/oauth-tokens.json`, `ELEVENLABS_API_KEY` (VOICE_ID/MODEL_ID unset → code defaults ok), `MIAI_B2B_ALLOWED_DOMAINS=myinstantai.com` + 1 allowlisted email.
Missing (need OAuth apps registered by MyInstantAI/deploying org, redirect `https://miaiweb-production.up.railway.app/api/oauth/callback`): **Microsoft** (M365/Teams/Outlook), **Shopify, Xero, QuickBooks, Zendesk, Notion, Todoist**. Docs: `docs/CONNECTOR_OAUTH.md`.

## Prior session context (all deployed via PRs #182/#183/#184)

- Flagship/suite link remaps (27/27 verified), voice two-way loop fixes, connector 401 UX, `/catalogue/:id` gating. Repo public, secret-scanned clean. 33 merged branches pruned; 25 unmerged remain.
- Tooling gotchas: `shell_command` has a hard ~10s wall regardless of `timeout_ms` sometimes — run long jobs with `nohup … > log &` and poll the log. GitHub GraphQL rate-limits: prefer `gh api -X POST repos/.../pulls` / `-X PUT .../pulls/N/merge`. Broad greps in `~/Projects/zara` time out — scope narrowly. zara workspace is read-only; local `*.db` files there are 0-byte placeholders (real DB is server-side).

## OPEN ITEMS (priority order)

- **A. Finish E2E repair PR** (above) — the handover acceptance signal.
- **B. Final handover acceptance pass** — sign in (allowlisted Google) → flagship cards → Setup → studio loads → connect Google connector → Voice Studio (long utterance, barge-in, 15s silence recovery) → embed snippet on test page. Needs the user in the loop.
- **C. Smaller backlog:** wallet mock→HTTP before real billing; GitHub repo topics+homepage; `next lint`→ESLint CLI migration; docs sprawl consolidation; CRM: rotate seeded admin password; CRM playground deploy is per-browser localStorage (offer server-persisted if wanted).
- **D. PARKED by user:** Takealot Copilot tools gap on zaraai.digital (`mia_pk_takealot_copilot` answers but has no product tools; `mia_pk_agentic_commerce` works — reference). Diagnosis trail: prod DB records live on the VPS (local DBs empty); embed `run_chat_turn` routes to the Node sidecar only when `rec.catalogue_agent_id` set, else legacy no-tools path. Takealot record likely lacks catalogue_agent_id or its package lacks tools. Do NOT work on this unless the user re-opens it.

## MoveDigital CRM playground (separate repo, `~/Projects/movedigital-crm`)

- Playground `/dashboard/playground`; 6 agents on zaraai.digital (`mia_pk_…` keys in `lib/agents-config.ts`). 5/6 fully functional; Takealot parked (item D).
- E2E script (15/15 passing): `~/.lmstudio/scratchpads/h/crm-playground-test.mjs` (needs `@playwright/test` resolvable — run with cwd in marketplace repo). CRM dev: `npx next dev -p 3001`, login `malcolm@movedigital.africa` (seed pw in `prisma/seed.ts` — rotate before public).

## Verification commands

```bash
pnpm typecheck          # 8 packages, 0 errors
pnpm test:web           # 311/311
pnpm --filter @miai/web lint
pnpm build:web          # all pages
pnpm smoke:staging      # @smoke vs Railway — 15/15 with E2E_SESSION_SECRET, else 12 pass + 3 skip
pnpm handover:staging   # full acceptance pack
```
