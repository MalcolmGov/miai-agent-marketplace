# Session State — Pause Point 15 Sept 2026 (resume 16 Sept)

> Read this first when resuming. One-screen summary, then details.

## Where things stand (all verified live)

- **Production:** `miaiweb-production.up.railway.app` runs commit `bb1839c` (main). Health `ok`, Postgres hydrated, Redis (Upstash) ok, OIDC auth, OpenAI model mode, wallet still `mock` (intentional for staging).
- **Merged today:** PR #182 (voice studio two-way + flagship fixes + lint unblock) and PR #183 (broken agent links, voice loop quality, connector 401 UX, /catalogue gate). CI + Publish image + E2E staging all green on `bb1839c`.
- **Repo is public** (MalcolmGov/miai-agent-marketplace, Apache-2.0). Full git-history secret scan run — clean. No user-data files ever committed.
- **Branch cleanup done:** 33 merged remote branches deleted; 25 unmerged remain (may hold unique work — review before deleting).

## What was fixed today (for context, all deployed)

1. Flagship/suite/dashboard "Setup" links → 6 linked to nonexistent catalog families (`invoicing`, `operations`, `cold-email-lead-gen`) → soft-404. Remapped to real families (`procurement`, `bookkeeping`, `sales-qualifier`); fixed `FLAGSHIP_FAMILY_MAP` in `apps/web/src/lib/catalog.ts` (bad `procurement→operations` remap broke direct links). 27/27 links verified.
2. Voice Studio: full-utterance transcript accumulation (was dropping finalized segments), adaptive endpointing 900/1700ms (was flat 850ms), bounded mic auto-restart on Chrome's silent no-speech drops, barge-in hands mic straight over, removed hardcoded "Hello Malcolm", word-boundary intent matching (`with` ≠ `it`).
3. Connectors: all ActionsPanel API calls redirect to `/login?return_to=…` on 401 instead of dead-end "Unauthorized".
4. `/catalogue/:id` rewrite alias now gated like `/agents/:id` (it bypassed the studio login gate); test added in `apps/web/test/business-auth.test.mjs`.

## OPEN ITEMS (tomorrow, priority order)

### A. Connectors — env config, not code (blocks demos)
Google works in prod (business sign-in proves creds). Remaining providers need OAuth apps created + Railway env set. Redirect URI for ALL: `https://miaiweb-production.up.railway.app/api/oauth/callback`
- `MICROSOFT_OAUTH_CLIENT_ID/SECRET` (M365, Teams, email)
- `SLACK_OAUTH_CLIENT_ID/SECRET`
- `SHOPIFY_OAUTH_CLIENT_ID/SECRET`
- `HUBSPOT_/XERO_/QUICKBOOKS_OAUTH_CLIENT_ID/SECRET`
- Also confirm `MIAI_B2B_ALLOWED_EMAILS`/`MIAI_B2B_ALLOWED_DOMAINS` includes the demo sign-in account, else Google sign-in is denied.
- Verify in prod once signed in: `/api/oauth/status` shows `configured` per connector.

### B. Takealot Copilot tools gap (Zara platform repo)
Playground agent `mia_pk_takealot_copilot` on zaraai.digital answers but has NO product tools ("I don't have a product lookup tool"). Backend gap in `~/Projects/zara` (Python monorepo; key registered in a SQLite DB there, no tool config in `embed/` or `agent_platform/`). The Autonomous Shopping Agent (`mia_pk_agentic_commerce`) DOES have working commerce tools — use it as the reference. Task: wire `search_takealot_catalog`, `verify_dc_stock`, etc. for the Takealot agent in the Zara backend.

### C. Final handover acceptance pass (before showing MyInstantAI)
Sign in (allowlisted Google) → each flagship card → Setup → studio loads → connect a Google connector → Voice Studio: speak a long multi-clause sentence (no cutoff), barge in mid-reply (mic hands over), stay silent 15s (mic recovers) → embed snippet on a test page.

### D. Smaller backlog
- Wallet still `mock` in prod — flip to HTTP adapter + billing webhook before real charges (dual-flag guards are deliberate).
- Repo metadata: add topics + homepage URL on GitHub.
- `next lint` deprecated (removed in Next 16) — migrate via `npx @next/codemod@canary next-lint-to-eslint-cli`.
- Docs: `CLAUDE.md` session-state + `CURSOR_*`/`CLAUDE_*` sprawl — consolidate session logs under `docs/sessions/`.
- CRM (see below): rotate seeded admin password before any public deploy; playground "Deploy" is per-browser localStorage — offer server-persisted global deploy if wanted.

## MoveDigital CRM playground (separate repo, `~/Projects/movedigital-crm`, main, clean)

- Playground at `/dashboard/playground`; 6 agents hosted on `zaraai.digital` via `mia_pk_…` keys. All 6 boot valid + chat-verified live today (5/6 fully functional; Takealot = item B above).
- E2E test script (Playwright, 15/15 passing) saved at `~/.lmstudio/scratchpads/h/crm-playground-test.mjs` — copy into the CRM repo tomorrow if wanted. Run: boot CRM `npx next dev -p 3001` (3000 is taken by the marketplace dev server), login `malcolm@movedigital.africa` (seed default pw in `prisma/seed.ts` — rotate it), then run the script with `@playwright/test` resolvable from the marketplace repo.
- CRM DB: Neon Postgres (us-east-1), `DATABASE_URL` in `.env.local`. NextAuth credentials auth.

## Repo map

| Repo | Local path | Role |
|---|---|---|
| miai-agent-marketplace | `~/Projects/miai-agent-marketplace` | White-label marketplace + runtime (this repo). Deploys to Railway on main push. |
| miai-agents | `~/Projects/miai-agents` | 500-agent roster / catalog source (main). No `invoicing`/`operations` family exists — link remaps were correct. |
| MoveDigital-CRM | `~/Projects/movedigital-crm` | CRM command center + agent playground. |
| zara | `~/Projects/zara` | Zara agent platform (Python) serving zaraai.digital + the 6 playground agents. |

## Verification commands (run after any resume change)

```bash
pnpm typecheck          # 8 packages, 0 errors
pnpm test:web           # 311/311
pnpm --filter @miai/web lint
pnpm build:web          # 54/54 pages
pnpm smoke:staging      # Playwright @smoke against Railway
```
