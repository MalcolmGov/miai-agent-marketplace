# Session State — 16 Sept 2026 (handover hardening)

> Read this first when resuming. One-screen summary, then details.

## Where things stand (verified live)

- **Production:** `miaiweb-production.up.railway.app` — health `ok`, Postgres hydrated, Redis (Upstash) ok, OIDC auth, OpenAI model mode, wallet `mock` (intentional for staging). Live at `4c69d77` (this session's fixes); always confirm with `/api/version`.
- **Repo:** this arc: #203 forge resilience · #204 dedicated forge package · #205 Sarah flagship voice · #206 spoken-language selector · #207 fleet controls (clickable KPIs + Disable/Delete) · #208 outbound-fetch + cleared-knowledge fixes · #210 deterministic unconfigured reply · #211 e2e pilot-facet selector · **new family: Collections & Debt Recovery (103 families × 5 markets = 515 indexed SKUs)**.
- **Railway:** CLI authenticated; project `robust-enthusiasm` / production / `@miai/web`. App-code merges auto-deploy (~3–4 min, Dockerfile builder); docs-only merges SKIP (watch paths). `railway deployment list` to watch.

## Verified in prod this session

- **Fleet controls (PR #207), 12/13 checks green** via a minted E2E session: disable → embed key 401, rentals show `embedRevoked:true`, re-enable → 200, delete → 200 then 404, delete kills the key (404), PATCH without body → 400. (The "13th" was my probe expecting 401 where the platform correctly returns 404 for a deleted agent — still a rejection.)
- **Website knowledge crawl works** (`POST /api/knowledge/crawl` on `movedigital.africa` → `status:ready`, 4 pages, real title; embed then grounds on it).
- **Cleared profile live behaviour:** cleared + no sources → fixed localized reply (EN/ES/FR verified), **0 model calls, 0 tokens, nothing invented**; cleared + sources → model grounded on those sources only (verified: the reply quoted the uploaded fixture); restored knowledge → normal grounding (`sales-development-rep` round-trip).
- **OAuth connectors:** 10 providers configured and their start-flows redirect correctly with `client_id` + `redirect_uri` + `state` (Google ×6 incl. YouTube, HubSpot, Slack, Calendly, Spotify). 8 await app registrations: Microsoft (m365/teams), Shopify, Xero, QuickBooks, Zendesk, Notion, Todoist — env names reported by `/api/oauth/status`.
- **Acceptance pack vs prod:** `@smoke|@functional|@uat|@handover` = **80 passed / 3 skipped / 0 failed** (the 2 initial failures were stale `/Go-live 100/` selectors after the polish renamed the facet to "Go-live" — fixed in #211).
- **Deploy pipeline:** `/api/version` reports the last *deployed* commit, not main's HEAD. Poll it, don't assume.

## Fixed this session (PR #208, merged `94ddaae`)

1. **Outbound fetch was dead in production.** `safeFetch` (website knowledge crawl, webhook dispatch, MCP calls, vendor APIs) hidden-imported `undici` — not a declared dependency, absent from the standalone image → every call failed with `SSRF blocked: undici unavailable for DNS-pinned fetch` (it worked locally because `undici` is present transitively). **This is why tenants could not add their website as knowledge.** Replaced with node's own `http`/`https` client: DNS pinned to the validated IP (anti-rebinding), SNI/cert/Host stay on the hostname, redirects never followed, timeout signal preserved, gzip/deflate/br decoded. `pinnedRequest` exported for tests (local server: GET/POST/gzip/redirect/abort/204).
2. **A cleared business profile served the catalogue example on live channels.** Rentals are seeded with the package example knowledge (sales-closer = "Brightline Studio"); clearing the field fell back to it, so an embedded widget introduced itself as a fictional business. Cleared (empty/whitespace) now means cleared on live channels: not-configured notice + a policy banning example content; uploaded sources stand alone; untouched rentals keep the demo fallback; studio sandbox unchanged. Also fixed the second leak: `{{business_name}}` in the package system prompt was materialised from the package example — `runTurn` now derives template vars from the tenant's effective knowledge, neutral ("this business") when it names no example.
3. **(PR #210) Deterministic reply when nothing is configured.** With the example gone, the model invented a business of its own ("Zanzibar Widget Works — anti-gravity umbrellas, $777" — which turned out to be a *test fixture source* someone had uploaded, not a hallucination, but the risk is real). A live turn with an empty profile and no sources now answers a fixed, localized notice (8 languages) with **no model call and no debit**, still recorded in history; cleared + sources still runs the model on those sources. Studio knowledge-panel copy matches.

Checks: connectors 59/59 · web 312/312 (incl. new `knowledge-cleared-live` regression) · runtime 182/182 · typecheck 8/8 · lint · build:web · eval smoke + static green · prod acceptance pack 80/3/0.

## For the user (open items)

1. **Sales Closer knowledge** — the user's workspace copy still holds the seeded example (it answers "Brightline Studio"). Clear it in the studio (Knowledge → *Clear template* → Save) to get the honest "not set up yet" reply, then add MoveDigital content: paste into the field, upload a file, or add `movedigital.africa` as a website source (crawl is working as of #208). Note: a fresh rent re-seeds the example — editing after renting is the intended flow.
2. **OAuth provider registrations still pending** (redirect URI `https://miaiweb-production.up.railway.app/api/oauth/callback`): Microsoft, Shopify, Xero, QuickBooks, Zendesk, Notion, Todoist (env names in `/api/oauth/status`). Configured and verified: Google (6 services incl. YouTube), Slack, HubSpot, Calendly, Spotify. See `docs/CONNECTOR_OAUTH.md`.
3. **Handover acceptance pass** (needs a human, signed-in): allowlisted Google sign-in → flagship card → Setup → studio → connect a Google connector → Voice Studio (long multi-clause utterance no cutoff; barge-in; 15 s silence recovery; one non-English utterance) → embed snippet on a test page.
4. **Before real billing:** wallet `mock` → `http` (`@prod` boot-gate asserts `walletMode:http` — fails until flipped). CRM: rotate seeded admin password.

## Collections & Debt Recovery family (shipped — user green-lit this session)

- 6 packs: `collections` (ZA source) + `us/eu/africa/asia/oceania-collections`, each with market conduct
  law (US FDCPA/Reg F 8am–9pm + 7-in-7 caps; EU GDPR/UCPD + national codes 08:00–20:00; Africa
  POPIA/NCA 08:00–20:00; Asia PDPA-style 09:00–20:00; Oceania APP/NZ + ACCC/ASIC windows) and clean
  local instalment amounts.
- 9 tools: `verify_customer`, `get_account_statement`, `get_repayment_options` (read) +
  `capture_promise_to_pay`, `send_payment_link` (financial), `capture_dispute`,
  `capture_hardship_review`, `mark_do_not_contact` (immediate, no confirmation), `handoff_to_human`.
- 16 evals; static suite 0 drift / 0 high issues; mock runtime 50% (same band as sales-closer/SDR —
  the mock model can't drive domain tools; the live model does).
- Registered: families 102→103, index 510→515, presets regenerated (+6), constants/copy/gate/e2e specs
  updated; integrity gate green.
- Generator fix: the emergency-number substitution (`\b000\b`) corrupted money (`R25,000` → `R25,911`);
  now guarded with digit lookarounds. Existing packs keep their old artefacts.
- Authoring sources (reusable): `~/.lmstudio/scratchpads/h/collections-package/` (parts + `bundle.mjs`),
  `patch-collections.mjs` (per-market compliance/amounts), `register-collections.mjs` (surgical index).

## Backlog / parked (user aware)

- **Per-language native voice map** (voice ID per language) — noted upgrade path after the language selector.
- **Takealot Copilot tools gap** — PARKED by user; do not re-open.
- Marketplace polish rounds 1–3 shipped (hub 11.6k→3.2k px, badge sweep, setup/connector declutter).

## Voice Studio (shipped earlier in this arc — keep working)

- Flagship voice = **Sarah** `dOH0XAoGHoc4a487cs6i` (user-supplied; en-US) + Railway envs `ELEVENLABS_VOICE_ID` / `ELEVENLABS_MODEL_ID=eleven_turbo_v2_5`. Stability 0.5 matches the approved samples. `language_code` forwarded only for non-English.
- Turn-taking: endpoint 3000 ms + <5-word fragment guard; "Tap to Send" submits the transcript; mic STT / replies / synthesis in 8 languages (`VOICE_LANGS`).

## Tooling & gotchas

- **Minted E2E session** for prod checks: `E2E_SESSION_SECRET` = Railway `OAUTH_TOKEN_SECRET` (`MIAI_SESSION_SECRET` unset on Railway), cookie `miai_business_session`, sub `e2e-smoke-user`. Not a backdoor — signature/audience/expiry + allowlist all enforced.
- Scratchpad probes (`~/.lmstudio/scratchpads/h/`): `fleet-verify.mjs` (fleet controls), `knowledge-probe.mjs` (create agent → crawl site → check grounding → delete), `oauth-probe.mjs` (per-provider readiness + start-flow URL), `cleared-knowledge-live.mjs` (clear → ask → restore on a live agent), `voice-e2e.mjs`, `closer-knowledge-check.mjs`.
- Live-channel knowledge semantics (as shipped): rental knowledge **undefined** → package example (demo); **""** + no sources → deterministic localized notice, no model; **""** + sources → model on sources only; **non-empty** → that text. Studio sandbox (`/api/chat`) always falls back to the example.
- `shell_command` often hard-caps ~10 s — run long jobs detached (`nohup … > /tmp/x.log &`) and poll.
- Prefer REST over GraphQL for GitHub (`gh api -X PUT repos/.../pulls/N/merge -f merge_method=squash`). Branch from `origin/main` — squash merges make stale local bases.
- `~/Projects/zara` is read-only; broad greps there time out.

## Verification commands

```bash
pnpm typecheck           # 8 packages
pnpm test:web            # 312/312
pnpm --filter @miai/connectors test   # 59/59
pnpm --filter @miai/runtime test      # 182/182
pnpm build:web && pnpm --filter @miai/web lint
pnpm eval:smoke && pnpm eval:suite:static
# E2E vs prod (authenticated half needs the secret):
E2E_SESSION_SECRET=<railway OAUTH_TOKEN_SECRET> PLAYWRIGHT_BASE_URL=https://miaiweb-production.up.railway.app pnpm handover:staging
```
