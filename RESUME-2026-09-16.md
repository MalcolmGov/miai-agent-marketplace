# Session State — 16 Sept 2026 (post E2E-repair merge)

> Read this first when resuming. One-screen summary, then details.

## Where things stand (verified live)

- **Production:** `miaiweb-production.up.railway.app` — commit `bb1839c` (see "Railway deploys" below — functionally identical to current main `aa48185`), health `ok`, Postgres hydrated, Redis (Upstash) ok, OIDC auth, OpenAI model mode, wallet `mock` (intentional for staging).
- **Repo:** main @ **`aa48185`** — PR #185 (E2E acceptance-suite repair) merged; CI + E2E staging green on main.
- **Railway deploys:** auto-skip for non-app changes (watch paths) — verified in `railway deployment list`: docs-only and e2e/docs merges show SKIPPED, app-code merges deploy ~3–4 min (Dockerfile builder). `/api/version` reports the last *deployed* commit, not main's HEAD.
- **Railway CLI:** authenticated (`malcolmgov24@gmail.com`), project `robust-enthusiasm` / production / `@miai/web`. Inspect env with `railway variables --json | jq -r 'keys[]'` (never print values). List deploys: `railway deployment list`.
- **Zara VPS** (embed/agent host, NOT the marketplace): `ssh aria` (46.224.42.163), app `/opt/aria`, containers `personal-assistant-agent` (:8000) + `zara-agent-runtime` (:8787).

## DONE this session — E2E acceptance-suite repair (PR #185, merged)

Root causes: (1) catalogue moved `/` → `/agents` after the B2B pivot; (2) gated surfaces ignore mock x-roles under OIDC.

- `e2e/auth.ts` (new): mints the standard HS256 business-session cookie (aud `miai:business:session`, same shape as `signBusinessSession`) from `E2E_SESSION_SECRET` (= target's `MIAI_SESSION_SECRET`, or `OAUTH_TOKEN_SECRET` fallback — **on Railway `MIAI_SESSION_SECRET` is unset, so use `OAUTH_TOKEN_SECRET`**) + optional `E2E_BUSINESS_EMAIL` (default `e2e-smoke@myinstantai.com`, covered by `MIAI_B2B_ALLOWED_DOMAINS=myinstantai.com`) / `E2E_BUSINESS_SUB`. Not a backdoor: signature, audience, expiry and allowlist all verified server-side. Also `targetAuthMode()`, `sessionHeaders()`, `authenticateContext()`, clean skip constants.
- All gated specs session-gated (API contracts, studio UI, hero matrix, refusal path, studio-setup, partner journey, diligence console pages); mock-role-only tests skip under OIDC; catalogue dialog test scoped to `#catalogue`; b2b wizard test now exercises the **real OIDC resume flow** (finish → Google bounce → `/get-started?resume=1` auto-submit); stale `shell-mode-business` testid (removed in #170) replaced with `app-sidebar` + business-nav assertions.
- CI: `e2e-staging.yml` passes `E2E_SESSION_SECRET`/`E2E_BUSINESS_EMAIL` from secrets when present. Docs: `TESTING.md` (authenticated-tests section), `PROD_E2E.md`, `CONNECTOR_OAUTH.md` corrected.

**Verification (against prod):** full pack `@smoke|@functional|@uat|@handover` **80 passed / 3 skipped / 0 failed** with secret (was 50/33); `@smoke` without secret **12 passed / 3 skipped**; CI on main E2E staging step green (12/3); typecheck 8/8, test:web 311/311, lint clean, build:web clean.

## For the user (quick actions)

1. **Optional but recommended — light up authenticated CI:** add GitHub repo secrets `E2E_SESSION_SECRET` (= Railway `OAUTH_TOKEN_SECRET` value) and optionally `E2E_BUSINESS_EMAIL`. Without them CI runs the public surface (12 pass + 3 clean skips) — still green, just thinner.
2. **Item B — handover acceptance pass (needs user, signed-in):** allowlisted Google sign-in → each flagship card → Setup → studio loads → connect a Google connector → Voice Studio (long multi-clause utterance no cutoff; barge-in hands mic over; 15s silence → mic recovers) → embed snippet on a test page.
3. **OAuth providers still pending app registration** (redirect URI `https://miaiweb-production.up.railway.app/api/oauth/callback`): Microsoft, Shopify, Xero, QuickBooks, Zendesk, Notion, Todoist. Configured today: Google (6 services), Slack, HubSpot, Calendly, Spotify. Details: `docs/CONNECTOR_OAUTH.md`.
4. **Optional backlog:** wallet `mock`→HTTP before real billing (deliberate staging state; `@prod` boot-gate asserts `walletMode:http` — it will fail until flipped); GitHub repo topics/homepage; `next lint`→ESLint CLI migration; docs consolidation; CRM: rotate seeded admin password; CRM playground deploy is per-browser localStorage.

## PARKED by user

- **Takealot Copilot tools gap** (`mia_pk_takealot_copilot` on zaraai.digital answers without product tools; `mia_pk_agentic_commerce` works). Prod agent records live on the Zara VPS; embed routes to the Node sidecar only when `catalogue_agent_id` is set. Do not re-open unless asked.

## MoveDigital CRM playground (separate repo, `~/Projects/movedigital-crm`)

- Playground `/dashboard/playground`; 6 agents on zaraai.digital (`mia_pk_…` keys in `lib/agents-config.ts`). 5/6 fully functional (Takealot parked).
- E2E script (15/15): `~/.lmstudio/scratchpads/h/crm-playground-test.mjs`. CRM dev: `npx next dev -p 3001`, login `malcolm@movedigital.africa` (seed pw in `prisma/seed.ts` — rotate before public).

## Tooling gotchas

- `shell_command` sometimes hard-caps at ~10s regardless of `timeout_ms` — run long jobs detached: `nohup … > /tmp/x.log 2>&1 &` then poll with short `sleep 9; tail` calls.
- GitHub: prefer REST (`gh api -X PUT repos/.../pulls/N/merge`) if GraphQL rate-limits bite.
- Broad greps in `~/Projects/zara` time out — scope narrowly; its local `*.db` files are 0-byte placeholders (real data is server-side).
- `zara` workspace is read-only for the assistant.

## Verification commands

```bash
pnpm typecheck          # 8 packages, 0 errors
pnpm test:web           # 311/311
pnpm --filter @miai/web lint
pnpm build:web
# E2E vs prod (add the secret for the authenticated half):
E2E_SESSION_SECRET=<railway OAUTH_TOKEN_SECRET> PLAYWRIGHT_BASE_URL=https://miaiweb-production.up.railway.app pnpm handover:staging
pnpm smoke:staging      # public surface only when the secret is unset
```
